# Documentation images

`run-workspace-approval.png` — the README's lead image. The frame to grab: the run
workspace mid-run with the **flash approval card** in the right rail (walkthrough v2,
~2:08) — streaming build log on the left, `AWAITING APPROVAL` on the status card, the
approval card's Approve & Continue / Review Diff / Reject on the right. It is the one
frame that shows both halves of the product at once: the agent working, and the human
gate it stops at.
