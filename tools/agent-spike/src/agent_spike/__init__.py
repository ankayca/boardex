"""Boardex agent-loop spike (docs/RUNNER_AGENT_V0_SPEC.md, wire layer excluded).

Proves: two-phase agent loop, harness meta-tools, tool-call interception with
the gate-floor amendment, and fixture-format event recording that replays in
the full UI via the mock runner.
"""

__version__ = "0.1.0"
