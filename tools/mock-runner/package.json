{
  "name": "@boardex/mock-runner",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Node + TypeScript mock runner replaying the BME280 fixture per BIBLE §5.3/§5.6 (HTTP command API + WebSocket event stream).",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@boardex/contract": "*",
    "ws": "^8.21.0"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1",
    "vitest": "^3.2.7"
  }
}
