# Decision Log
2026-07-07 — Adopted docs/BIBLE.md v1.1 as source of truth for UI, contract, and mock runner. servers/ remains cofounder-owned; orchestrator service (servers/boardex-runner) will implement the §5 contract.
2026-07-07 — BIBLE v1.2: added run.iteration_started event (fix-loop iteration was unrepresentable); removed nextAction from RunSummary (UI-derived, single source); BenchStatus devices carry backend device_id.
2026-07-07 — T0.5: status-badge colors derived from D14 (§6.2 fixes risk/verdict mappings but not per-status colors): completed=green, failed/stopped=red, plan_ready/awaiting_approval/diagnosing=amber (needs the human), draft/planning/running=neutral. StatusDot: online=green, offline=amber (degraded-bench warning per §7.2), error=red.
2026-07-07 — T0.5 correction (review FIX_FIRST): status 'diagnosing' moves amber→neutral. Amber statuses are now exactly plan_ready and awaiting_approval — the states where a human action exists; diagnosing is the agent working, so it reads neutral. Also: solid-amber "High" risk badge text moves white→text-primary (#1C1917) for small-text contrast (critical stays white on red).
2026-07-07 — BIBLE v1.3 (§5.4): RunView gains riskSummary (populated from run.plan_generated; undefined before the plan exists); rejected reading raw events outside reduceRun (planEvents.ts) to preserve D5's single-derivation guarantee. Rule going forward: if the UI needs data RunView lacks, extend RunView via the reducer — never read the event list directly. Wire contract (events/commands/json-schema) unchanged.
2026-07-07 — Fail-closed rule for approval surfaces: unresolved/unknown safety context (profile, checklist, limits) blocks approval explicitly; it is never treated as an empty or default value. Found via T1.3 review (D12 vacuous-true bypass).
2026-07-08 — Cofounder sign-off: StepKind/step.log model confirmed against orchestrator plans (yes); fixture-notes bench-check plausible; UI shell = web now, Tauri wrap later (D1 confirmed, supersedes ARCHITECTURE.md's Electron line).
2026-07-08 — BIBLE v1.4 (§5.4, T2.1): RunView.logsByStep values become {stream, line}[] (the reducer was flattening away the step.log stream, making §7.3's per-stream log tabs underivable) and RunView gains iterations[] ({iteration, reason, firstStepIndex} from run.iteration_started) so the workspace can place the iteration divider without reading raw events. Both reducer-only, per the v1.3 rule; wire contract (events/commands/json-schema) unchanged.
2026-07-08 — RunView gains endedAt from the terminal event's envelope ts; terminal runs show frozen total duration instead of no elapsed figure (T2.2 finding).
2026-07-08 — v1.6: diagnosis↔fix-approval linkage derived in the reducer (fixApprovalId); one approve surface per pending approval (T2.2 review F1/F3).
2026-07-08 — Disabled-until-event is in-session UX only, deliberately not reload-persistent: post-reload re-click is idempotent-safe (409-swallow) and safety lives in the runner's approval gating, not the button state (T2.2 review F2, accepted).
2026-07-09 — T3.1: decode-table failure semantics per fixture-notes.md §3 (binding): a transaction is failed — fail bg tint — ONLY on nack_at="address"; the final-byte NACK of a master read is normal I2C and renders plainly. Deep links resolve fail-closed: unknown artifact ids and T3.2-tab kinds land on Checks with an explicit notice. lib/config.ts now falls back to process.env.VITE_RUNNER_URL (browser-safe via globalThis) because vitest snapshots import.meta.env before vi.stubEnv runs — this restores the integration tests' ephemeral-port seam (composer integration was failing on a clean tree).
2026-07-09 — T3.1 review F1: i2c.py boundary mirror (~40 lines) accepted; undercount-only + fixture-swap tripwire; queued as a decode-artifact shape proposal (per-transaction indices) for the runner contract.
2026-07-09 — T3.2 diff rendering: hand-rolled unified-diff parser + minimal per-line C tokenizer, no new dependencies (react-diff-view would bring split-view/widget machinery, a lenient parser that can't fail closed, and default green/red line painting plus a non-token Prism theme). Colors per D14: added/removed diff lines do NOT use green/red (reserved for pass/fail semantics) — removals render dimmed on the neutral badge tint behind a "-" gutter, additions full-contrast behind "+"; syntax highlighting stays inside §6.1 (accent for keywords/preprocessor, secondary/italic for comments and strings).
2026-07-09 — T3.2 rollback + downloads: Rollback is the §7.4 MVP affordance only — enabled while the run is non-terminal, disabled with an explanatory tooltip when terminal; clicking (enabled) surfaces a notice that revert is runner-side and that no rollback command exists in the contract yet (adding one follows §10.5, not a UI stub). Artifact downloads go through a same-origin Blob typed by artifact.mimeType and named `{artifact.id}` + kind extension (the mock runner's own storage mapping), because the anchor download attribute is ignored on cross-origin URLs.
2026-07-10 — T3.3: ApprovalCard's "Review Diff" is navigation into the Evidence Detail Code Diff tab (deep link at the run's latest code_diff artifact), not a second diff surface — one renderer, one route, so a diff seen at the gate is the diff seen in evidence. Fail-closed like the band's Open Diff: no code_diff artifact yet ⇒ inert control with a tooltip, never a link into an empty tab. The fixture emits art_diff_iter1 (seq 12) before the flash gate opens (seq 20), so the control is live at that gate. The Diagnosis Card's failed-check links now carry the same evidence-linking-law gate (§4) as the band chips and the Checks table: an artifactId with no artifact.created in RunView renders inert.
2026-07-09 — T3.3 review: Review Diff links the run's latest code_diff; the 'diff at the gate is the diff the gate is about' property holds only because edit steps immediately precede flash gates. A proposal→artifact link (Approval.proposal gains diffArtifactId?) is queued as a wire-contract proposal for the runner conversation — not patched UI-side.
2026-07-10 — T4.1 Board Profile Builder: (a) new profiles are keyed client-side (`bp_<base36 time><random>`) because POST /board-profiles takes a whole BoardProfile and §4 leaves ids opaque — the mock runner keys its runs the same way; (b) Validate Profile matching is exact, within a device kind: the profile's instrument string resolves against BenchStatus.devices `id` first, then the device `name` case-insensitively — the detected-device picker writes the stable id, so picked instruments always resolve and hand-typed ones resolve only when literally right (the canned profile's prose names, e.g. "Kingst LA2016 (sigrok)", therefore read as missing); a matched-but-offline/error device is `degraded`, not missing; validation is advisory and never blocks Save (benches change); (c) knownQuirks has no §7.5 section, so the form carries it through untouched rather than blanking it on save; (d) fail-closed: /boards/:id that cannot load its profile shows the blocked pattern, never an empty editable form that would POST blanks over a real profile; (e) the top bar gains a "Boards" link — §7.5 is an MVP screen with no other entry point.
2026-07-10 — T4.1 review (FIX_FIRST): (F1) the fail-closed profile guard keys on "no profile in hand" (data === undefined), not "the last fetch failed" — a failed background refetch of an already-loaded profile now shows an inline amber notice and keeps the form and its unsaved edits, because the overwrite-blanks danger the guard exists for has already passed; (F2) the mock runner's canned Nucleo-F303RE profile now stores the bench's stable device ids rather than prose names, so a healthy bench validates found/found and the picker's output is what the canned data models — free-text-does-not-resolve stays covered by its own test; (F3) supersedes yesterday's (b) clause below in spirit for profiles only: no 409 optimistic-concurrency message on board-profile saves. §5.3 gives 409 a meaning only for commands invalid against a RUN's state, and no runner implements profile versioning; a stale-edit message would assert a concurrency model that does not exist. If profile versioning ever lands it arrives via §10.5 (bible → contract → mock), with the message.
2026-07-10 — T4.2 bench flows: (a) the found/degraded/missing derivation moves to apps/ui/src/lib/benchReadiness.ts — three screens read it (ValidationPanel, composer readiness + its repeat at the approval gate, Home's indicator), so it can no longer live under pages/boards; useBenchStatus moves to lib/ for the same reason. (b) The three states get three sentences, written once in that module: found says nothing (a green dot and the device id are the message), degraded says "<name> is on the bench but offline/in error (<runner detail>)" with the device's own StatusDot (amber offline, red error), missing says "<reference> was not found on the bench" with no dot at all — there is no device to have a state. The fixes differ (plug it in vs. edit the profile), so the copy must. (c) The composer/gate warning lists bench-level unhealthy devices whether or not the profile claims them (§7.2 says "offline devices" — the bench's own report), then the profile's unresolved references; a device that is both appears once, as degraded, because the bench's report is the more specific fact. Its heading is "Bench degraded" only when a real device is unhealthy — a profile that merely names something nonexistent gets "Bench references not found", since the bench is fine. (d) With no profile resolved there is no claim to be missing, so only bench facts render. (e) BIBLE §7.1/§7.2 updated for both, per §9.3.
2026-07-10 — T4.2 Home bench indicator placement: a one-line amber link under the runner-offline banner slot, above the run list — NOT in the top bar next to the runner pill (the pill is the shell's, and §7.1 gives it no bench detail; the bar is also rendered on every screen, which would make an advisory Home affordance global). It is suppressed while the runner is offline: that snapshot is stale by definition and the offline banner already says the bench cannot be seen — two contradictory amber lines are worse than one true one. Advisory only; it gates nothing.
2026-07-10 — T4.2 rail seam (Kerem's ruling: folded in): the workspace's Board Context rail (§7.3) resolves instruments by REFERENCE through lib/benchReadiness, not by device kind — found renders a green dot labelled with the device's stable id, degraded keeps the device's own StatusDot (amber offline, red error), and a reference no device answers to renders no dot at all (there is no device whose state it could report) plus the shared amber "<reference> was not found on the bench". The rail is what an operator watches during a run, so an unplugged analyzer must not read like a profile naming an analyzer that does not exist. Serial keeps its kind lookup — §4 makes it a port + baud, not a device the profile names — and with no bench snapshot every instrument still reads offline against its own reference: never an assumed online, and never a false "not found" against a bench we cannot see. BIBLE §7.3 updated.
2026-07-10 — T4.2 review F1–F5 (FIX_FIRST). (F1) The bench snapshot is only as true as the connection that delivered it: globalStream now exposes WsClient's status, and useBenchStatus drops the snapshot whenever the global socket leaves 'open' — making benchStore.clear() live in production rather than test-only. benchStore gains a `generation` counter that scopes the GET /bench fallback's query key, so a response cached under a dead connection can never be served for the current one. Home's attention line therefore gates on holding a snapshot that postdates the current connection, not on /health: HTTP liveness and stream liveness are different questions, and only the second one knows whether the analyzer came back while we were not listening. A downed runner now suppresses the line without a special case. (F2) BenchIssue is a discriminated union on `status`: {status:'missing'} structurally cannot carry a deviceState, because a reference nothing answers to has no device whose health a dot could report. (F3) That impossibility is now pinned in the rendering too — the missing-state tests on BenchWarning and ValidationPanel assert no StatusDot of any colour. (F4) Rail rows label with the device NAME, not its registry id: the rail is 280px of glanceable context and "Kingst LA2016" is what is recognisable on the bench; the stable id moves to the details drawer. (F5, Kerem's ruling) SUPERSEDES the "never an assumed online" rule recorded in the 2026-07-10 rail-seam entry above (left unedited as history): with no bench snapshot the rail renders a fourth presentation, unknown — no dots, plain names, one neutral "Bench status unavailable." line, as the composer already did. The old rule assumed pessimism was safe, but painting every instrument amber reports a healthy analyzer as unplugged every time the socket blinks. Never an assumed anything. A readable bench with no device of a kind is still "not found" — that is an answer, not a silence. BIBLE §7.3 updated for F4 and F5.
2026-07-10 — BIBLE v2.0 (T5.0 contract conformance hardening, wire changes; header bumped, §5.1/§5.3/§5.7/§4/§7.1/§7.3 edited). Individual rulings below.
2026-07-10 — T5.0/F1: envelope-first parsing made normative (§5.1). "Unknown types must be ignored" cannot mean dropped when seq is gapless — a dropped frame parks the reducer on a permanent gap and turns reconnect into a loop. Every well-formed envelope counts toward seq continuity; contract gains parseWireEvent/WireEvent (an IgnoredEvent is a parse-time tag, never a wire field), GetRunEventsResponseSchema parses envelope-first per event, and reduceRun accepts WireEvent. Ruling within the ruling: a KNOWN type whose payload fails the catalog parse is treated identically (kept for seq, carries no state) — typing it would make the reducer read fields that are not there, dropping it would fabricate a gap; the emitted commands.schema.json keeps GetRunEventsResponse as strict Event[] because looseness is the reader's obligation, not the producer's license.
2026-07-10 — T5.0 timestamp edge: IsoDateTimeSchema accepts naive (zoneless) ISO 8601 (§4 says "ISO 8601", not "ISO 8601 with zone"); producers SHOULD emit UTC with Z (§5.1 note). Wire-visible, part of v2.0.
2026-07-10 — T5.0/F2: the three structured artifact content schemas promoted into packages/contract/src/artifacts.ts + json-schema/artifacts.schema.json. protocol_decode reconciled to the boardex-logic pipeline's ACTUAL shapes: annotations are parse.py::parse_annotations output ({raw,start?,end?,decoder?,text}), transactions decode/i2c.py::parse_transactions output; the fixture decode artifacts were regenerated to that shape (raw is the sigrok -A line, round-tripped through the real parser — byte-identical) and the invented start_sample/end_sample keys removed from fixture-notes.md. UI decode/diff readers now import the contract schemas; the decode table's time column derives from annotations[].start and degrades to an em dash when a line has no sample prefix.
2026-07-10 — T5.0/F3: §5.7 normative run-status transition graph added. Key MUSTs: terminal states have no outgoing edges; a runner blocking on plan approval MUST have reported plan_ready (the plan gate is visible state, not an implementation detail); awaiting_approval only with a pending approval; diagnosing exits to awaiting_approval or failed.
2026-07-10 — T5.0/F4+F7: run.completed/run.failed/run.stopped join the global stream (§5.3) — a run ending via its dedicated terminal event must reach the dashboard without a redundant run.status_changed (the fixture gains no events); the mock forwards them globally and Home invalidates ['runs'] on all three. F7: the mock seeds a schema-valid RunSummary at POST /runs time (title/boardProfileId from the fixture's run.created, status 'draft', updatedAt now) — GET /runs during the pre-run.created replay window used to serve an empty-string updatedAt no IsoDateTime parse accepts.
2026-07-10 — T5.0 Kerem's adjudication: mid-run approvals keep profile-independence — the approval surface derives from the event stream and the bench's own report only, never from re-resolving profile references; AND the §7.2 bench-degraded warning repeats on every approval that proposes hardware actions (implemented in the workspace right rail via benchIssues(bench, null); advisory, never gating). §7.3 updated.
2026-07-10 — T5.0 label change (Kerem's ruling): the Home next-action label for awaiting_approval is "Review approval", replacing §7.1's "Approve flash" — the row cannot know WHICH approval is pending (flash, fix, or something a real runner invents), so the label names the user's action instead of guessing the proposal. §7.1 updated.
2026-07-10 — T5.0/F5 (reducer-only; wire unchanged; bible §5.4 updated): legal-ordering reconciliation. Early step.completed/step.failed and approval.resolved buffer until their entity arrives; a check the evidence law downgraded upgrades back to its wire verdict when its artifact lands; warnings are computed for what the stream never reconciled — so a mid-stream prefix truthfully shows the violation-in-progress and the next event can dissolve it. RunView.warnings now surfaces in the workspace StatusCard as a compact amber count expanding to the reducer's messages (amber per D14: a warning needing the human).
2026-07-10 — T5.0/F8: ProfileForm's detected-device picker reads the bench through lib/useBenchStatus instead of a private ['bench'] query — the T4.2-F1 liveness rule (a snapshot dies with the connection that delivered it) is a property of the SOURCE, so every bench surface must share the one source; a page-local query was the class of bug. Validate Profile still re-fetches on click ("validated" means "checked just now").
2026-07-10 — T5.0/F9: the mock gains --fail-variant (or FIXTURE=fail): a second authored fixture, bme280_run_001_fail.jsonl, identical to the base story through seq 68 (iteration-2 flash), after which the corrected address STILL NACKs (wire byte 0xEC — the fix landed; the hardware is at fault), checks come back pass/fail/fail, and the run ends in run.failed with no further fix approval. Its decode artifact round-trips the house parsers like the base ones; its own validation test guards the shared-prefix property.
2026-07-10 — T5.0/F10: three UI-driven integration tests against the live mock — the failed-terminal workspace (--fail-variant: Failed badge, no Stop Run, evidence retained pass/fail/fail), Stop Run through the ConfirmDialog to the stopped workspace, and Reject at the flash gate to the stopped workspace.
2026-07-11 — T5.0 conformance review FIX_FIRST F1–F4 (bible §5.4/§5.5/§5.6 + changelog updated). (F1) §5.1 makes run.created's seq-1 position a convention, not a promise — an ignored envelope may legally precede it. reduceRun is now reduceRun(events: WireEvent[]): RunView | null: null is the valid, EMPTY view for a stream holding no known event yet; the missing_run throw is retained — and extended to every known type, not just the run.* events — for a KNOWN-typed stream that starts wrong. Defense in depth behind it: runStore.ingest catches reduction errors (recorded on RunEntry.reduceError), holds the view stable and never lets a throw escape the WS message handler; ws.ts wraps the replay flush so a throw cannot strand replayInFlight=true (state resets, the socket cycles, and 'reconnecting' is the surfaced error state); the mock's stop() emits the fixture's run.created first when a stop beats it (the curl edge), so no reachable mock stream is unreducible. (F2) art_serial_log_iter2f and the fail fixture's seq-77 step.log regenerated to the strings the evidenced firmware prints: the iteration-2 driver has no NACKF handling, so an address NACK surfaces as iteration 1's i2c1_wait timeout lines ("timeout waiting for TXIS (read setup)") — the UART cannot distinguish the corrected address on dead hardware from the wrong address, only the decode can. The diff is deliberately NOT extended with NACKF handling: correct fix, faulty hardware — the minimal diff is the evidence. Asserted line-for-line against the iter1 log. (F4) ProfileForm's picker is pinned to useBenchStatus by test: a stubbed source must feed the picker and no bench query may register behind it, so a regression to a private ['bench'] query fails.
2026-07-12 — T5.2 (+review F1) socket lifecycle: two terminal predicates on purpose. The fast detach fires only on the DEDICATED terminal events (run.completed/failed/stopped), never on the derived status — §5.3 lets run.status_changed(terminal) legally precede the dedicated event (the mock's stop path emits exactly that pair), and detaching on status strands the log's last event (caught by the D5 deep-equal proof: live lastSeq 21 vs replayed 22). The fallback release (F1) covers a status_changed-only terminal stream, where the fast path never fires: when the socket would enter 'reconnecting' — a drop, or the heartbeat cycling a now-silent connection — while the reduced view is terminal, the client releases it instead: one final HTTP replay from lastSeq catches any stranded tail (e.g. a dedicated terminal event still in flight), then the status settles 'closed'. Invariant either way: a terminal view never surfaces 'reconnecting', and no socket outlives the run it carries.
2026-07-12 — T5.2 review F2, replay-retry error classes: failures of GET /runs/{id}/events during the run stream's primary replay split by determinism. HTTP 404 means the runner does not know the run id — retrying cannot change the answer — so the stream fails closed as a distinct 'not_found' state and the workspace route renders "Run not found on the runner" with a link back to Runs: no amber bar, no retry loop, no socket, no further requests. Network errors and 5xx stay transient: backoff + 'reconnecting', exactly like a socket outage. Only 404 fails closed for now — the remaining 4xx have no §5.3-defined meaning for this GET, and inventing UI states for them would guess semantics the contract does not give. BIBLE §5.3 gains the 404 sentence, per §9.3.
2026-07-12 — Sprint 6 (UI excellence) adopted; sign-in/sign-up rejected for MVP (D13, no server-side identity until Track 3); model selection gated on runner-capabilities contract proposal; Documents/Sources panel supersedes Deferred items 1/6 partially — display, not generation.
2026-07-12 — §5.6: mock rebases fixture timestamps to replay start; fixture files stay authored-time.
2026-07-12 — T6.1b/c frame adoption: persistent app shell (240px sidebar collapsible to a 56px icon rail — collapse state in memory, not storage; 48px context top bar carrying route-derived titles and page actions). §6.3's three-zone workspace split keys on CONTENT width (container query, ≥1240px) rather than the viewport, because the sidebar's width would otherwise overflow the right rail on laptop screens; per-page content widths (Home/Boards 1040px left-aligned, composer reading column, workspace center capped 940px). Stop Run becomes outline-danger at natural width (red fill only under hover intent — D14 unchanged: red is stop only). BIBLE §6.1 token block and §6.3 updated to the T6.1 reality (label type step, elevation levels, motion tokens, focus ring, tabular numerals).
2026-07-12 — T6.3 stage 0 (regression fix): the profile-details Drawer and Stop-Run ConfirmDialog now portal to document.body. Both overlay primitives were rendered inline inside `position: sticky` workspace rails (T6.2b); sticky establishes a stacking context, so the overlay's z-50 was scoped to its rail and a sibling sticky rail (later in DOM, same z-auto grid level) painted over an open drawer. Portaling lifts the §6.1 overlay elevation out of every sticky ancestor unconditionally — fixed at the stacking-context level, not by escalating z-index. Regression test: a Drawer mounted inside a sticky container renders its dialog at body level.
2026-07-12 — BIBLE v2.1 (T6.3 Documents & Sources — additive wire changes; PR to the backend owner). All new fields optional, wire contractVersion stays boardex-contract/0.1 (bumping it would falsely trip §10.4's mismatch banner against a v2.0-era real runner; v2.0's wire changes set the precedent of a stable version string). Changes: (1) BoardProfile.documents?: BoardDocument[] where BoardDocument = {id,label,kind:'datasheet'|'schematic'|'reference',mimeType}; (2) routes GET /documents/{id} (content, MIME per mimeType) + /documents/{id}/meta (BoardDocument); (3) MeasurementCheck.sourceDoc?: {documentId, locator?} beside the surviving free-text sourceRef (the fallback rendering); (4) runner capabilities riding along for T6.6 — GET /health.capabilities?.models?, CreateRun.model?, Run.model? (echoed). Reducer unchanged (optional fields pass through on their entities). JSON schema re-emitted (BoardDocument, DocumentKind, GetDocumentMetaResponse; Run/MeasurementCheck/BoardProfile/HealthResponse/CreateRunRequest grow the fields). §4/§5.3 + header changelog updated; contract round-trip tests added for every new shape.
2026-07-12 — T6.3 design ruling: sourceDoc on the check SUPERSEDES the T6.3 sprint blurb's originally-sketched `source_excerpt` artifact kind. A citation is a resolvable pointer into profile-owned reference material (a BoardProfile.documents[] id + locator), not a per-run evidence artifact — so no new ArtifactKind is added; the §4 ArtifactKind enum is unchanged. §T6.3 blurb reconciled to the built design.
2026-07-12 — T6.3 mock authored documents: the canned Nucleo-F303RE profile carries two documents — a BME280 datasheet excerpt (markdown; §5.4.1 addressing table with the 0x76/0xEC wire bytes + §6.2 100 kHz timing spec) and schematic pin-mapping notes (markdown; PB8/PB9 I2C1 AF open-drain, SDO→GND selecting 0x76, LA2016 D0/D1 tap) — both authored consistent with the fixture story and servers/ house facts, held as string constants in tools/mock-runner/src/data.ts and served verbatim. The datasheet's two H2 headings slugify (GitHub-style) to the exact fixture locators. Both fixtures' i2c_clock/device_ack checks gain sourceDoc at those locators (payload-only; event counts + shared-prefix unchanged). health advertises capabilities.models:['mock-model']; a create-run model is echoed onto run.created's Run.model (session-level, the one emit seam). Both authored docs are markdown; the PDF native-embed branch (Stage-2 step 7) is covered by a UI unit test with an application/pdf document — serving is mimeType-driven, so a real schematic PDF drops in later with no code change.
2026-07-12 — T6.3 stage 2 (UI, same branch during the v2.1 PR review). (a) Evidence drawer gains a Sources tab (after Checks) listing the profile's documents; markdown renders via the T5.1 parser (report/markdown.ts) in a document renderer that adds GitHub-style heading slug ids and highlights the located heading; PDF (mimeType application/pdf) via a native <object> embed with a fail-closed fallback link; other types best-effort as plain <pre>; every fetch fails closed (loading/unfetchable-retry/empty). (b) sourceDoc deep links via a new evidenceDocHref → /runs/:id/evidence?doc=<id>&loc=<locator>; the drawer opens the Sources tab at that document and locator. Checks table Source cell becomes the link when the sourceDoc resolves to a known profile document, else plain sourceRef (never a dead link). Report (ReportView) links a sourceRef TEXT to the Sources tab using the SAME whole-run exact-match currency as artifact labels — a sourceRef in prose stays plain (fail-closed); the fixture report happens not to bold the datasheet ref, so no live link there, which is honest. (c) Board Profile Builder gains a Documents section: editable id/label/kind(select)/mimeType rows (profileDraft.documents refactored from carried-through BoardDocument[] to editable DocumentRow[]); metadata only — content upload is out of scope, noted in the section hint. Each present row requires id/label/mimeType; a documents-less profile still omits the key. (d) Composer model select is feature-detected on GET /health.capabilities.models: rendered ONLY when >1 model (default = first), the choice rides POST /runs; absent or single → no UI, no model sent (mock advertises exactly ['mock-model'], so the demo shows no select). (e) The run's echoed model (Run.model) renders in the StatusCard meta and the ReportPage header, only when present. Verified headless against the mock (Playwright): Stage-0 drawer sits over a scrolled workspace with nothing bleeding through; Sources tab lists both docs and renders the datasheet; the i2c_clock Source link deep-links to the highlighted "Timing specifications" heading; the builder shows both document rows; the composer shows no model select — zero console errors.
2026-07-12 — T6.3 review FIX_FIRST (F1–F3, one commit on stage 2). (F1) Second-citation dead link: the evidence drawer's derived-state reset key now includes the locator (`${artifactParam}|${docParam}|${locParam}`), and SourcesTab's seed comparison is keyed on `doc|loc` (builder's choice, per the review) rather than doc alone — so a second citation into the SAME document at a different locator re-opens Sources and re-highlights even after the user browsed to another tab or another document by hand. Two regression tests ship with it: the reviewer's probe (navigate ?doc=X&loc=A → switch to Checks → navigate ?doc=X&loc=B, asserts Sources re-selects and the new locator highlights) and the fixture-shaped case (click check 1's source link, then check 2's — same document, different locators — both land highlighted). (F2) Bible drift (§9.3): §7.4's tab enumeration gains Sources; §7.5 is now 7 sections (Documents added). (F3) Datasheet excerpt voice: the run-narrating sentence ("…is the fix the run applies") reworded to reference-material voice ("Composing the address as (0x76 << 1) = 0xEC is the standard correction for this class of fault") — the document reads as profile-owned reference, not demo narration.
2026-07-12 — T5.3 prep (Ahmet, per Kerem's integration one-pager): tools/mock-runner/src/server.test.ts parameterized with RUNNER_BASE_URL (§10.4 item 2) — external mode skips mock-only cases (fixture-exact counts, fail-variant/degraded/slow runners) and derives artifact ids from the driven run's own stream; fixture-mode behavior unchanged (12/12 green). Root eslint.config.js gains a '.venv/**' ignore: pip's vendored JS inside the backend venv was failing npm run verify on any machine with the Python env bootstrapped. Both edits touch UI-owner files and await Kerem's sign-off. servers/boardex-runner now exists (scripted orchestrator, runnerKind "real", fake-bench + real-bench modes); the shared conformance suite passes against it (8 passed, 4 skipped).
2026-07-13 — AgentBench port (BENCH=agent, servers/boardex-runner; sanctioned exception to the servers/ read-only rule, scoped to boardex-runner only, landing as a PR the backend owner gates). The tools/agent-spike loop transplants behind the existing engine seams, none of which changed shape: declare_plan parks on the engine's EXISTING plan-approval future (HTTP-driven, `POST /runs/{id}/plan/approve`), every intercepted hardware call parks on `_approval_gate` (rejection → the engine's stopped path; the MCP tool provably never invoked), all events go through EventLog (append-path schema validation comes free), artifacts through ArtifactStore, the RECORD tee unchanged. Stop is a hard cancel: the base stop seals the log, then the agent task is cancelled at its next await. One AgentBench per run (audit HIGH-1's singleton mistake is structurally avoided); `/bench` serves a static snapshot with no devices — hardware lives behind the per-run MCP servers, so there is nothing to scan (and nothing to block the loop, audit HIGH-2). The interception gate floor ships per the spec §3.1 amendment (audit MEDIUM-5): flash_/reset_/erase_/recover_/write_ prefixes + composites + first-line description scan, hardcoded, removable by no configuration — a falsey safety.flashRequiresApproval still gates, and that branch is now tested. Model plumbing: AGENT_MODELS (env, comma-separated; default the spike's tested openrouter/anthropic/claude-sonnet-4.6) → /health capabilities.models; CreateRun.model must be in the list — an unknown model is rejected 409 with {error} and NO currentStatus (there is no run whose status could be reported; §5.3's 409 body shape is defined for run-state commands) — and the model actually used (chosen or first-listed default) is echoed onto Run.model. Keys are provider-standard env vars read by LiteLLM at call time; never logged, stored, or emitted. Agent deps (litellm/mcp + litellm's fastapi/orjson lazy imports) live in an optional [agent] extra with lazy imports — fake/real deployments and CI install none of them. Known honest gap, flagged not absorbed: a plan-phase abort (agent never plans / malformed twice) emits run.failed while status=planning, an edge §5.7 does not draw — the pre-existing engine catch-all (a bench.plan() crash) does exactly the same today, so the seam is the backend owner's to rule on, not this port's to invent around.
2026-07-13 — File tools are harness-owned (AgentBench ruling): list_files/read_file/write_file are meta-layer tools implemented in the runner, scoped to the run profile's repoPath, NOT MCP server tools — boardex-target/boardex-logic expose no file editing by design (the spike's predecessor proof got file tools from its editor host), and granting an LLM filesystem reach beyond the task repo through a hardware MCP server would invert the trust boundary. Every write_file records a contract code_diff artifact ({files:[{path,reason,diff}]}) so edits render in the UI diff viewer and are citable evidence.
2026-07-14 — Bench bring-up pre-hardware fixes (servers/boardex-runner, backend-owner-gated PR; sanctioned servers/ exception scoped to boardex-runner). (HIGH-1) Run mutex: benches carry `exclusive` (RealBench/AgentBench true — one physical probe+analyzer; FakeBench false — per-run instances share nothing); RunnerApp.create_run returns 409 {error:"bench busy", currentStatus:<active run status>} while any run is non-terminal on an exclusive bench, serializing shared-hardware runs the audit showed collide on _evidence/_session_id. (HIGH-2) bench_status() is now async, offloaded to the executor and cached ~5 s: the pyOCD USB enumeration + `sigrok-cli --scan` (≤20 s) it runs no longer blocks the event loop on GET /bench or a global-WS handshake (UI treats bench status as advisory). (MED-5) RealBench.flash_approval() no longer returns None on a falsey safety.flashRequiresApproval — flashing mutates hardware and always requests approval; profile config may add friction, never remove the floor (parity with AgentBench interception). Test added on the falsey branch (tests/test_real_bench.py) — the fake fixture never exercised it, which is why the bug survived.
2026-07-14 — Bench bring-up bugfix (servers/boardex-target/pyocd_ops.py, found flashing a real board via BENCH=agent): pyOCD's default FileProgrammer writes flash erase/program progress bars ([====…]/[---|---…]) to sys.stdout. boardex-target runs as a stdio MCP server, where stdout IS the JSON-RPC transport, so the bars corrupted framing — the runner's mcp_host stdout reader threw pydantic "Invalid JSON" and the flash tool call's response was lost, failing the run. Fix: pass an explicit no-op progress callback (_silent_progress) to FileProgrammer so pyOCD emits nothing on stdout (its logging still goes to stderr, which is safe). Regression test asserts flash() passes a callable, silent progress callback. This is the audit HIGH-2-adjacent "genuine tool output the fake host never exercised" seam, in a new spot (stdout pollution, not loop-blocking).
2026-07-14 — Runner operability (servers/boardex-runner): added BOARDEX_BOARD_PROFILES=<json file> (a BoardProfile object or array) baked into state_from_env at launch for BENCH=fake|agent. Board profiles otherwise live only in runner memory (POST /board-profiles), so every restart silently dropped back to the default fake profile and an agent run created against bp_nucleo_f303re resolved to a repoPath (/bench/firmware/bme280-f303re) that doesn't exist → "Agent bench cannot start" at seq 2. Baking the profile into the launch survives restarts; test covers the fake branch.
2026-07-14 — Design constant §2.1, debug-control gating RULED destructive-only (interception.py docstring): the gate floor covers flash_/reset_/erase_/recover_/write_ prefixes + run_checkpoint/verify_bringup composites; halt_target/resume_target/run_until/step/set_breakpoint/set_watchpoint and register/memory reads execute ungated — nothing irreversible escapes. halt_target joins RISK_NAMES only for a bench that can be mid-actuation (dangerous frozen output); the BMP180 bring-up bench cannot, so it stays out. §2.2 naming contract re-affirmed: any future destructive MCP tool must carry a risk prefix or a first-line mutation verb — the classifier depends on it.
2026-07-13 — AgentBench id/planIndex resolutions (the audit's breaking assumptions, decided): (a) step ids are seq-derived — st_{kind}_{runSuffix}_{seq} — because the scripted engine's st_{kind}_iter{n} scheme collides the moment an agent builds or flashes N times in one iteration; approval ids keep the engine's existing seq-derived scheme, and N approval gates per iteration is normal, not an error. (b) RunStep.planIndex stays required on the wire (contract unchanged); the agent binds it via the harness-only optional `_plan_index` tool parameter (stripped before validation and MCP dispatch, clamped to the declared plan, defaulting to the last bound row) — plan rows may be revisited or never claimed, which the UI already tolerates. (c) There is no discrete evaluate stage: check.evaluated fires whenever the agent calls record_check, and the evidence law is enforced at that moment (artifactId must resolve within the run). (d) Iteration segmentation is the declare_iteration meta-tool, harness-counted against profile.safety.maxIterations — a bound, never a prompt suggestion.
2026-07-14 — T6.5 demo source (Kerem's ruling): the demo replays a recorded agent run bundled CLIENT-SIDE (D5 payoff — replay is reduction). The AgentBench recording Kerem intended (records/agent-run-1) came back a 2-event run.failed with no artifacts — the live tier-1 run is blocked on API credits (see port-report.md / memory). Per Kerem's decision the demo therefore bundles the authored fixture packages/contract/fixtures/bme280_run_001.jsonl (90 events, full run: plan → 2 approvals → 6 checks → step.failed → diagnosis → iteration 2 → completion → report; 16 artifacts) — which is exactly what BIBLE §8 T6.5 names ("replaying the fixture"). The bundle is a generated copy under apps/ui/src/demo/data/ (events + artifact text + the canonical Nucleo-F303RE profile, copied from tools/mock-runner/src/data.ts — the real profile the fixture references, not invented). Swapping in a genuine recording later (§10.3) is a re-generate of those data files, zero code change.
2026-07-14 — T6.5 demo architecture. (a) /demo is its own top-level route family (/demo, /demo/evidence, /demo/report) OUTSIDE the app Layout — its own read-only DemoShell with a top-bar "Demo — replaying a recorded agent run" badge and an exit, so no api-bound Sidebar/TopBar/CommandCenter sits in the demo path. (b) Playback drives a DEDICATED run-store instance (createRunStore(), not the app singleton) event-by-event, paced by delayMs compressed to ~90s with long gaps capped — "driven through the run store exactly like a live stream", so reduceRun and every workspace surface behave identically; the demo view is passed as the existing `view` prop. (c) Command safety: WorkspacePage's only api-coupled child was StatusApprovalRail — refactored to take run commands from a new RunCommandsContext (lib/runCommands) and the bench via prop (threaded from WorkspacePage) instead of importing lib/api / useBenchStatus. Live mounts an api-backed provider (RunPage); the demo mounts a local provider whose stop = exit-demo and resolveApproval = skip-to-the-recording's-own-resolution. StateConflict/ApiError extracted to lib/apiErrors so the rail imports the error types without the api singleton. The demo command + playback modules import no lib/api at all — structurally command-incapable, same never-executes spirit as the command palette; a test asserts it. (d) Evidence links: EvidenceBand, StatusApprovalRail, DiagnosisCard, ChecksTab build evidence/report hrefs from an EvidenceBaseContext (default `/runs/${runId}`, demo `/demo`) so the reused surfaces deep-link within the demo shell; no provider = unchanged live behavior (existing tests untouched). (e) Artifact reads: the existing fetch layer (api.getArtifactText/Blob) gains a demo-source branch backed by a lazily-registered bundled map (setDemoArtifactSource), installed by a single read-only demo bridge module and cleared on demo exit — offline, no network, the command path never imports it. (f) Guided tour: 5–6 callouts anchored to real playback moments (plan appears, approval gate, agent log streaming, a check lands, diagnosis, report), advancing on the moment or Next, dismissible, shown-once via module memory (not storage). D14 untouched; tokens only; zero changes to reducer/contract/transport.
2026-07-15 — T6.5 review FIX_FIRST, demo-gate command honesty (Kerem's ruling). The demo cannot keep the live rail's promises at a gate, so it stops pretending to. (F1) Reject: the bundled recording was APPROVED, so there is no rejected ending to replay and synthesizing a `run.stopped` would be a fabricated event (§5.2 events are truth). The demo's resolveApproval now branches on status — `rejected` freezes playback and surfaces an honest notice ("In a live run, Reject ends the run as Stopped. This recording was approved — exiting demo."), then exits; only `approved` fast-forwards to the recording's own resolution. (P5) Stop Run in the demo skips the live ConfirmDialog and exits directly: the confirm copy ("ends immediately as Stopped … Evidence retained") describes a live run, not a replay that merely leaves. Threaded via a `demoMode`/`confirmStop` flag from DemoPage → WorkspacePage → StatusApprovalRail → StatusCard; live behavior unchanged (flag defaults off). (F2) noApiImport.test rewritten as a recursive denylist over src/demo/** (both `from '…'` and `import('…')` of lib/api and lib/liveRunCommands), documented single exception demoArtifactSource; verified against two planted probes. (F3) The palette never-executes test additionally spies globalThis.fetch and the runStore mutators. (P4) The sidebar's "Watch a demo run" now gates on runsQuery.isSuccess, matching Home's first-use hero.
2026-07-15 — T6.5 review §9.3 doc-debt pass (one commit; the bible catches up to code that shipped reducer-/UI-only across sprint 6, no wire changes). (F4) Per-line log timestamps: RunView.logsByStep is `{stream, line, ts}[]` (bible header v2.2 clause + §5.4), the ts being the step.log envelope's — so a batched `lines[]` frame's lines all share one timestamp. This is the only honest option: the contract times events, not individual lines, so per-line wall-clock would be invented precision. Feeds the LogViewer's optional timestamp column; wire contractVersion stays boardex-contract/0.1. (F5a) Verified / latest-execution-wins (Kerem's ruling, previously living only in progress.ts comments): the StatusCard progress reads "Verified", counting plan indices whose LATEST executed step succeeded — an iteration-2 re-open of a failed index drops it back out until it re-succeeds — so the bar means "steps proven", never "steps attempted". (F5b) Per-stream accent read-down: the selected log-stream tab carries a 2px accent underline at the seam (accent = the interactive selection, not a verdict); log text itself is never colour-coded — D14 holds, colour stays reserved for pass/fail/warn. §7.3's stream list corrected from four to the five §5.2 streams (agent/build/flash/serial/rtt). Bible also refreshed: §7 gains the /demo route family; §7.1 (pill→sidebar, New Run→top bar, two-action offline-capable hero); §6.2/§7.3 (per-line timestamps + find-in-log); §6.3 (non-scrolling h-screen frame + sticky rails, sticky off below 1240px). (F6) §6.1 gains --color-scrim (text-primary at 40%, already in code), the composer type metrics (24px / 32px / −0.019em), and an ambient 2s motion line; the literal 2s of the active-step pulse is promoted to a --motion-ambient token in index.css (builder's choice) — same value, one less magic number.
2026-07-16 — T6.6 Settings + model selection (light loop, UI-only; zero contract changes — all fields exist since v2.1). (a) Runner base URL becomes a RUNTIME setting: lib/config resolves user override > VITE_RUNNER_URL > §5.6 default fresh on every call, so a change re-points the whole app — the api singleton resolves per request (its baseUrl is now a getter, nothing captured at construction), runStream/globalStream read getRunnerWsBase(), the shared global socket tears down and reconnects on a URL change (guarded by a base comparison so an unrelated setting never cycles a healthy connection), and useRunStream reconnects on a runner-URL version tick. Env stays the default when no user value is set — the dev / dev:real path is untouched; an explicit base passed to createApiClient (tests, the Test Connection probe) stays fixed. (b) Persistence is MODULE MEMORY (lib/settings), the SAME mechanism the sidebar collapse and demo tour-seen flag already use — deliberately NOT storage, so a user override DROPS ON RELOAD; matching the pattern this codebase established rather than introducing a new storage path, per the T6.6 constraint. The sidebar-collapse flag folds into lib/settings for one source of truth. (c) Test Connection classifies a /health probe into online / version-mismatch / degraded (ok:false) / offline (unreachable). D14 ruling (review F1): a failed probe is a WARNING to resolve, not a fail/stop — offline, mismatch, and degraded all render AMBER (dot + text + sr-only agree), and red stays reserved for pass/fail run semantics; only `online` is green. The degraded verdict category names a runner that answers but reports ok:false — reachable yet not ready, distinct from unreachable. (d) Model selection was already built in T6.3 stage 2 (feature-detected composer select on capabilities.models>1, Run.model echoed into the StatusCard meta + report header); T6.6 adds a read-only advertised-model list to the Settings Model section. BIBLE §7 gains a §7.7 Settings screen stub (the MVP screen set goes Six → Seven). [Entry recovered 2026-07-19: this line and the §7.7 stub shipped in b1e8e1a, which was stranded unmerged on docs/setup-guides — see the 2026-07-19 Sprint 7 review F2 entry.]
2026-07-17 — §10.3 ruling (Kerem): the authored fixtures (packages/contract/fixtures/bme280_run_001*.jsonl) STAY as test infrastructure — dozens of tests pin their exact properties (event counts, 6-step plan, iteration 2, the three named checks, 9–13 min span; scripts/validate_recording.py encodes that gate), so they cannot be swapped out. Real hardware recordings become demo/validation ASSETS instead, replayed via a new FIXTURE_FILE override on the mock runner — SUPERSEDING the original intent to swap the authored fixture for a real recording, because real recordings' shapes vary run to run and pinning the suite to one would be brittle. FIXTURE_FILE was named in the demo-asset strategy but had never been built; this lands it (tools/mock-runner: loadFixtureFile + a per-recording artifacts/ dir + dynamic rekey off the recording's own run.created id rather than the hardcoded FIXTURE_RUN_ID; a relative FIXTURE_FILE resolves against the repo root so it works from any cwd; precedence over --fail-variant). First tracked keeper: records/bmp180-run/, via a gitignore carve-out (/records/* ignored, !/records/bmp180-run/); the rest of records/ stays local-only. Scope note: building FIXTURE_FILE was beyond the original task, which assumed the mechanism already existed.
2026-07-17 — First real-hardware agent run findings (records/bmp180-run, 2026-07-16) and the fixes they motivated (branch agent/hardware-run-tuning, servers/boardex-runner, PR-gated). The honest story from the STREAM, not the report's prose: the firmware functionally worked (RTT chip_id=0x55, temp=31.9 C, press=91754 Pa) but the run ended run.failed on "turn bound exceeded: max_turns=40" with only TWO of ~six planned checks recorded via record_check (build_exit_code, chip_id_rtt — both pass); the partial report over-claims PASS on four checks (temperature/pressure/device_ack/scl_frequency) that were never recorded. Four fumbles → four fixes in prompts.py + agent_bench.py: (1) 5 turns burned on read_memory rejecting a quoted hex address ('0x40021000' is not an int) → the memory/debug tool arg schemas are now stated exactly (read_memory(device_id, address:int, length:int), hex-as-decimal, device_id vs session_id), pulled from boardex-target's real definitions; (2) the final check went unrecorded because the report was written first → the evidence-law encoding now orders record_check for EVERY registered check BEFORE write_report; (3) the report claimed verdicts it never recorded → the encoding now forbids asserting a report verdict that no record_check backs; (4) an SCL frequency-window check is the wrong metric on a clock-stretching bus (the BMP180 drags the mean toggle rate to ~28 kHz while the programmed clock is correct) → planner guidance now prefers pulse-width / stretch-aware metrics. Turn budget 40→60 (DEFAULT_MAX_TURNS + the server.py env default that actually takes effect; AGENT_MAX_TURNS override unchanged; a config-pin test asserts the new default). Validation: the recording is structurally sound (186 events, seq gapless 1..186, every line schema-valid, 15/15 artifacts resolve with matching sizeBytes, structured bodies valid) and reduces via reduceRun to a run.failed RunView with the two checks present; it REPLAYS through the UI via FIXTURE_FILE to terminal with the decode table (5 rows), code diff, serial logs (chip_id=0x55) and report all rendering, zero console errors. NOTE the original task framing ("all functional checks passing, one check unrecorded") was inaccurate to the stream and is corrected here and in records/bmp180-run/README.md.
2026-07-18 — Sprint 7 P0 visual system (bible §6 rewritten to v2.3; doc-first per §9.3). Source: the external design review of the full product (docs/design/Boardex_MVP_UI_Design_Review.docx — 7:09 walkthrough, prioritized backlog; constraint frame identical to ours: light theme, D14 unchanged, flow unchanged, refinement not expansion). Shipped as ONE integrated system change, explicitly not item-by-item, because isolated tweaks preserve the inconsistencies being fixed. The system: three-layer surface hierarchy (canvas #F7F7F8 / nav #FBFBFC / white primary surfaces) replacing white-on-white; full color re-point (borders #E2E3E7/#D2D4DA, text #17171A/#5C6068, accent #5B4CF0 with derived hover #4A3BD8, pass #168A4A, fail #C73535, warn #A86D00, neutral badge #3F434B on #E9EAEE, scrim at 35%) with the old values RETIRED (a token test greps them out); rewritten type ladder (page 22, section/top-bar 15, card/step title 14 semibold, meta 13, metadata 12, code 12.5 mono, label 11 reserved for the run-state + risk capsules; 12px floor for state-bearing text); geometry on the 8px grid (card 8px, control 6px, buttons 36/40px, min target 32px, sidebar 208px, rail 320px, drawer 840px cap 47vw, workspace breakpoint 1240→1208); motion EXTENDED with --motion-morph 280ms (FAIL→PASS icon morph + one restrained pulse) — fast/medium/gentle/ambient unchanged; shadows now exist only on floating layers (resting cards read by border + surface, --shadow-subtle retired); §6.2 Badge split into four classes (run-state capsule 11px / risk capsule / verdict icon-led 12px with icon ALWAYS present / inline step status icon + neutral text), with "Not recorded" defined as neutral-gray-with-dash, never red. Derived-not-specified values (accent hover, semantic bg tints, neutral badge pair) are the builder's derivations inside the designer's palette, noted in §6.1.
2026-07-18 — Sprint 7 P0 stage 4 ruling (Kerem): the dual-outcome coverage denominator. The task's "Validation coverage — N of M checks recorded, derived purely from RunView" was underivable for records/bmp180-run: the runner registers its declared checks harness-side only (declare_plan captures them; run.plan_generated emitted only plan+riskSummary — raw payload keys verified), and the four never-recorded requirement names exist nowhere in the 186-event stream, only in the report's prose. Ruling, three parts: (1) contract v2.4, additive per §10.5 and PR-gated to the backend owner — run.plan_generated gains OPTIONAL checks?: CheckExpectation[] {requirementId, description}; reducer exposes RunView.registeredChecks (verbatim) + terminalSummary (terminal event summary; terminal status_changed.reason as fallback; run.stopped sets none). The plan declares what is checked, checks record against it, coverage is the ratio — the denominator is NEVER parsed from plan prose or report markdown. Pairs with the hardware-run-tuning rule "record every registered check before write_report": same registry, two uses. (2) records/bmp180-run stays byte-identical (preserve acceptance evidence) and, predating v2.4, renders the honest fallback "2 checks recorded · no check registry declared" with NO fabricated Not-recorded chips — inventing a denominator is exactly the dishonesty the trust story exists to prevent. (3) A clearly-marked SYNTHETIC fixture (bme280_run_002_partial_synthetic.jsonl — authored, not a recording) declares six checks and records two, making the dual-outcome UI + four neutral-gray Not-recorded chips demonstrable and testable today; it is the stage-4 acceptance screenshot source, consistent with the §10.3 fixture ruling. The authored bme280 fixtures declare their three checks (identical prefix preserved for the fail variant's shared-prefix test).
2026-07-19 — Sprint 7 P0 review F4 ruling (Kerem): terminalSummary for stopped runs. For a stopped run the badge IS the story — the reducer suppresses the reason fallback when a terminal run.status_changed carries the generic "Stopped by user" boilerplate (the mock's stop sequence: status_changed(stopped, "Stopped by user") then run.stopped would otherwise render "Stopped · Stopped by user", saying it twice). Any NON-generic reason (e.g. "Approval rejected") is real information and survives as the summary; bare run.stopped stays summary-less as before (§5.4 amended). Same review, F2 finding recorded for the record: T6.6's amber-probe fix (b1e8e1a) was never merged — it is the stranded tip of docs/setup-guides (main took its parent a75ae13 only), so Settings' Test Connection verdicts regressed to red at the stage-1 migration, which rebuilt from the unfixed base. Not a migration re-break and not an incomplete fix; the Sprint 7 stage commits contain no StatusDot state remaps. Re-applied on sprint7/p0-visual-system with dot+text agreement pinned by tests.
2026-07-19 — Sprint 7 P1 token additions (bible §6.1 amended; doc-first per §9.3). Two new values, both required by the P1 evidence-drawer backlog (design review §5, figure 5) and both outside D14: (1) --color-accent-bg #ECEBFB, a light tint of the accent (#5B4CF0) — the RESTING background of a cited-source highlight (3px accent rail + this tint + a "Cited source" label). Accent is the one action/highlight color; a light tint of it carries no pass/fail/warn meaning, so a citation may wear it without touching green/red/amber. Same lightness family as the pass/fail/warn tints; pinned in tokens.test. (2) --motion-arrival 700ms, in the review's 600–800ms band — a one-shot "you landed here" wash slower than a state flip (medium 200ms) so it reads as settling, not flipping; used by the cited-source arrival, removed under prefers-reduced-motion like every animation. No other new values this pass — every other P1 surface reuses the v2.3 palette + motion tokens.
2026-07-19 — Sprint 7 P1 code-diff line tints (REVERSES the 2026-07-09 T3.2 ruling "added/removed lines do NOT use green/red"). Kerem's P1 framing: a changed line legitimately carries pass/fail semantics — an addition is the new/passing state, a removal is the old/removed state — so subtle pass-bg (add) / fail-bg (del) tints are D14-COMPLIANT, not decorative. The old ruling read added/removed as merely visual and routed them to the neutral tint to protect green/red; the reinterpretation is that the changed-ness IS the semantic. Tints only (pass-bg/fail-bg, existing tokens); the +/- gutters and per-line syntax highlighting are unchanged. Removed lines keep their dimmed foreground over the fail tint; added lines render full-contrast over the pass tint.
2026-07-19 — Sprint 7 P1 hero button height (Kerem approved). The first-run empty-state hero's two actions (New Run / Watch a demo run) render at the 40px button height to meet the design review's "38–40px hero buttons" spec (§3). 40px is the only in-band button-height token, so it is reused here as a SIZE choice for the empty-state hero — NOT a claim of gate-primary semantics. §6.1 reserves 40px's MEANING (plan approval, Approve & Continue, Approve Fix Plan), not the height itself; the hero borrows the height, not the meaning. No new token.
2026-07-19 — Sprint 7 P1 review F2 ruling (demo affordance, doc-debt). On a genuine empty state the sidebar carries NO "Watch a demo run" link — the first-use hero owns the demo affordance, and duplicating it would leave two demo entries competing on one screen. The sidebar's own quiet demo link (a play glyph + "Watch a demo run") renders only AFTER runs exist: RecentRuns returns null while recent.length === 0, so the link appears the moment the hero is gone. Chosen over the play-icon-only alternative — keeping a sidebar demo link on the empty state and differentiating it from the hero merely by a small play glyph; the play glyph still rides the after-runs sidebar link for visual consistency with the hero, but it is NOT what resolves the duplication (hiding-on-empty is). BIBLE §7.1's sentence is corrected: it had read the sidebar "mirrors the demo action when it has no recent runs, gated the same way" — the exact inverse of the shipped gate.
2026-07-19 — Sprint 7 P1 review F4 ruling (report tofu layer + a last-updated omission, doc-debt). (a) Tofu fold: a symbol missing from JetBrains Mono in a rendered report_md is fixed at the FONT-FALLBACK layer — the mono stack gains ui-monospace → SFMono-Regular → Menlo → Consolas → DejaVu Sans Mono ahead of the generic monospace (BIBLE §6.1), so each OS resolves a symbol-covering mono face (Consolas on Windows, SF Mono/Menlo on macOS, DejaVu Sans Mono on Linux) instead of the last-resort face that renders tofu — NEVER by rewriting or normalizing the report content. The report_md is the agent's evidence and must not be edited to make a glyph render; a font-layer fix also covers EVERY symbol a report can carry, not the one checkmark that prompted it. Chosen over normalizing the offending glyph in the markdown. (b) Folded in: the Boards list surfaces no "last updated" column because BoardProfile carries no timestamp field (§4 — unlike RunSummary's updatedAt); a derived or placeholder time would violate never-invent, so only real data shows and the omission is deliberate, not unfinished work.
2026-07-19 — Hardware run 2 (same BMP180 prompt, real bench) — findings and the no-keeper ruling. Iteration 1's bug was the GPIO base address: F4's AHB1 base 0x40020000 used where the F303 has GPIO on AHB2 0x48000000 — the agent diagnosed it and fixed it under a gated fix approval. Iteration 2 brought the bus alive but it still NACKed at a plausible address: the decode showed 0xDC, likely a one-bit misframe of 0xEE — unresolved, because the run died on the OpenRouter key's per-key spend cap (run.failed, litellm APIError). Validated in-stream before it died: 6/6 checks recorded before the diagnosis, the i2c_scl_high_pulse check spec in use, zero read_memory argument failures — the hardware-run-tuning PR's three fixes all visibly working. Ruling: the repo keeps exactly ONE flagship recording (currently records/bmp180-run); run 2 archives externally — no keeper. This entry is the motivating case the fault-discrimination prompt section (2026-07-24 below) cites.
2026-07-20 — Agent cost telemetry stays off the wire (v2.5 candidate). Per-turn litellm usage (prompt/completion tokens + Anthropic cache read/write detail via OpenRouter) is now captured by the runner and logged into the run's agent step.log stream — one quiet line per provider call, plus a run total on the report step. Deliberately NO contract change: a structured `Run.usage` field (per-run token totals the UI could render next to the terminal summary) is recorded here as a v2.5 note, to be added additively per §10.5 once the shape has survived a few hardware runs. Until then the log stream is the only carrier, so replaying old recordings stays valid and the UI needs no changes.
2026-07-21 — CI/CD + release infrastructure (backend-owner authored; UI-owner sign-off requested on the shared root .github/). .github/workflows/ci.yml runs three gates on push/PR: the hardware-free pytest matrix (Linux/macOS/Windows × Python 3.10/3.12/3.13 — all suites are USB-free by invariant, so CI-for-hardware stays deferred per §2.3 #9), the UI owner's `npm run verify` UNMODIFIED on Node 20/22 plus the Playwright smoke, and an arm-none-eabi build of examples/firmware. Releases are tag-driven (servers-vX.Y.Z → .github/workflows/release.yml): the four server packages version in LOCKSTEP via servers/VERSION + sync_versions.py, wheels/sdists are built, installed into a clean venv, smoked against a fake-bench boardex-runner (release_smoke.py: /health contract check + run creation + event replay), then published to a GitHub Release — no PyPI, no installers (§2.3 #7 untouched). Driver stance codified in docs/SUPPORT_MATRIX.md + boardex-doctor (boardex-core console script): no kernel drivers or vendor blobs ever ship; probes via pyOCD/libusb, LAs via system sigrok-cli, Linux probe udev rules now shipped in servers/boardex-target/contrib/udev/. Portability fixes in our domain only: mcp_host venv path (bin/ vs Scripts/), builder tests rewritten onto sys.executable. The stale Electron wording in README/ARCHITECTURE remains flagged, not fixed (open item for the UI owner).
2026-07-21 — servers/boardex-bench-agent deleted (was already hollow: only __pycache__ remnants, no sources, no pyproject — the agent loop lives in boardex-runner BENCH=agent since the runner port). mcp-agent/ (untracked clone of lastmile-ai/mcp-agent, the retired spike's substrate) added to .gitignore additively.
2026-07-24 — Layer-1 hardware fault discrimination (prompts.py only; agent/fault-discrimination). Run 2's iteration 2 ended with a LIVE bus still NACKing at a plausible address — the exact shape where an agent burns iterations rewriting correct code while the fault is physical (sensor power, pull-ups, wiring). SYSTEM_PROMPT gains a "Fault-domain discrimination" section: a signature table mapping bus evidence to the domain it indicates (silent bus + register-readback-verified config → wiring/probe/power, not code; a line stuck low → short/latch-up, unfixable in firmware; toggling-but-never-clean-high where RTT proves execution → pull-ups/signal path, the "no square wave" test; correct address byte + NACK on every attempt → device absent/unpowered/address-strapped, with the decoder-misframe caveat that a one-bit-late 0xEE reads as 0xDC; flash/probe failures like "target was not halted" → bench problem, one retry then report; only a wire that CONTRADICTS verified code intent is firmware), plus a hard protocol: before ANY second fix-iteration for a communication failure — register readback vs intent, capture during a known attempt, classify, declare_diagnosis naming the suspected DOMAIN (firmware / hardware / instrumentation) with the artifact cited; hardware/instrumentation domains end the run honestly with record_check on everything measured and a report naming the SPECIFIC physical checks (pins, pull-up values, power rails from the profile's connectionChecklist) — "what to check with a multimeter, not a fifth driver rewrite". Written as bench-engineering judgment, not rigid if-then ("signatures, not certainties; state your confidence and cite the capture"). Layer-2 companion deferred to v2.5: a structured fault-domain `category` field on declare_diagnosis, which would let agent_bench.py enforce the protocol behaviorally (e.g. gate a second declare_iteration on a domain-bearing diagnosis) — NO behavioral enforcement ships in this layer, prompts.py + a phrase-pin test only.
